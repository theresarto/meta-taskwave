# Meta TaskWave Dependencies
# Core framework requirements for distributed FaaS scheduling benchmark

# === Data Analysis & Scientific Computing ===
numpy>=1.21.0,<2.0.0
pandas>=1.3.0,<3.0.0
scipy>=1.7.0,<2.0.0

# === Platform Compatibility ===
# Windows compatibility for color output
colorama>=0.4.4,<1.0.0; sys_platform == "win32"

# Note: Core system uses only Python standard library modules:
# - asyncio (Python 3.7+ built-in)
# - json, csv (built-in)
# - time, datetime (built-in)
# - logging (built-in)
# - socket (built-in)
# - subprocess (built-in)
# - pathlib (built-in)
# - argparse (built-in)
# - uuid (built-in)
# - typing (built-in)
# - random, heapq, deque (built-in)
# - os, sys, shutil (built-in)

# Installation command:
# pip install -r requirements.txt